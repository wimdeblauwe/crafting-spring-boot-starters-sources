# Defaults activated when the 'prod' profile is active.
# Quieter logging; richer propagation list to feed the production tracing pipeline.
logging.level.com.example.requestid=WARN
request-id.propagate-headers=X-Correlation-Id,X-B3-TraceId,X-Cloud-Trace-Context,X-Amzn-Trace-Id
